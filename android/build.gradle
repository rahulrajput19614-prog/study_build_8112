buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        // ✅ Latest AGP compatible with Flutter
        classpath "com.android.tools.build:gradle:8.6.0"

        // ✅ Google Services for Firebase
        classpath "com.google.gms:google-services:4.4.2"
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
