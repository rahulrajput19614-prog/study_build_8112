pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
    plugins {
        id "com.android.application" version "8.6.0"
        id "org.jetbrains.kotlin.android" version "1.9.22"
        id "com.google.gms.google-services" version "4.4.2"
        // ❌ dev.flutter.flutter-gradle-plugin यहां नहीं लिखना
    }
}

include(":app")
