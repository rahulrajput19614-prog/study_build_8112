name: study_build
description: A scalable, student-focused education app.
publish_to: none
version: 1.0.0+1

environment:
  sdk: ">=3.1.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # 🔹 Core UI
  sizer: ^2.0.15
  flutter_svg: ^2.0.9
  google_fonts: ^6.1.0
  shared_preferences: ^2.2.2

  # 🔹 Networking (Render backend)
  http: ^1.0.0
  dio: ^5.4.0
  connectivity_plus: ^5.0.2

  # 🔹 Media & Input
  file_picker: ^6.1.1
  video_player: ^2.7.2
  image_picker: ^1.0.4
  camera: ^0.11.0+1
  speech_to_text: ^5.6.1   # ✅ Fixed namespace issue

  # 🔹 Charts & UI Feedback
  fl_chart: ^0.65.0
  fluttertoast: ^8.2.4
  cached_network_image: ^3.3.1

  # 🔹 Permissions
  permission_handler: ^10.4.0

  # 🔹 App Info & Utilities
  package_info_plus: ^4.1.0
  url_launcher: ^6.1.10

  # 🔹 Firebase (for update checker)
  firebase_core: ^2.24.2
  firebase_remote_config: ^4.3.4

  # 🔹 Google Generative AI
  google_generative_ai: ^0.4.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/images/

  plugin:
    platforms:
      android:
        package: com.mr.flutter.plugin.filepicker
        pluginClass: FilePickerPlugin
      ios:
        pluginClass: FilePickerPlugin
